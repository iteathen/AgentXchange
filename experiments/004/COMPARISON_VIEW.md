# Experiment 004 — Frozen Comparison View

This file is part of the cold packet.

## Source independence

`CORPUS_ALPHA.isg`, `CORPUS_BETA.isg`, and `CORPUS_GAMMA.isg` are independent top-level documents.

Their bare SIs occupy independent source-local SI namespaces.

Their stable relation labels also occupy independent source-local label namespaces.

Numeric coincidence across files supplies no sameness evidence.

## Allowed source-to-source mapping

For this experiment, the comparison view permits:

- alpha-mapping of source-local SIs by structural role;
- mapped-signature correspondence between source-local stable relation labels;
- comparison of ordered-incidence topology, repeated-SI sharing, dependency incidence, multiplicity, and boundary structure.

The view does not permit:

- arbitrary permutation of ordered incidence positions within one relation;
- deletion of a relation merely to improve a match;
- merging two distinct source-local SIs merely to improve a match;
- splitting one source-local SI into several merely to improve a match;
- treating numeric label similarity as evidence.

## Load-bearing structure

Treat as load-bearing unless independently shown otherwise:

- ordered incidence position;
- repeated use of the same SI within a source;
- distinction between different SIs within a source;
- relation occurrence multiplicity;
- direct dependency references between event SIs;
- source scope/boundary.

## Initial factorization

The direct relation occurrences in each file are the frozen initial extraction/factorization set.

Discovery may perform the bounded recursive role/factorization work required by the current Discovery Protocols when a residual or structural lead justifies it.

Do not assume the initial extraction is canonical.

## Target claim strength

The target is a structural common-core/correspondence result with explicit residuals and falsification.

Do not promote findings into:

- full-trace isomorphism unless every load-bearing residual is discharged;
- natural identity;
- identical operational meaning;
- identical authority semantics.

No NEI claim is requested by this experiment.
